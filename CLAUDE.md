# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

A GitHub Actions progress viewer — a Node.js project (inferred from the `.gitignore` template). The project has not been scaffolded yet beyond the initial commit.

## Setup

Once dependencies are introduced, update this file with:
- Package manager in use (npm / yarn / pnpm)
- How to install dependencies (`npm install`, etc.)
- How to run the dev server, build, lint, and tests
- How to run a single test file or test by name
